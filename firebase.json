{
  "database": {
    "rules": "database.rules.json"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR/functions\" install",
      "npm --prefix \"$RESOURCE_DIR/functions\" run build"
    ],
    "source": "functions"
  },
  "hosting": {
    "public": "hosting/build",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "predeploy": [
      "npm --prefix \"$PROJECT_DIR/hosting\" install",
      "npm --prefix \"$PROJECT_DIR/hosting\" run build"
    ]
  }
}
